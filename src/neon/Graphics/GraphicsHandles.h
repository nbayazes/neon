#pragma once

namespace neon {

// Handle to a mesh loaded on the GPU. Contains submeshes.
enum class MeshID : short { None = -1 };

// Handle to a texture resource
enum class TexID : int { None = -1 };

}
